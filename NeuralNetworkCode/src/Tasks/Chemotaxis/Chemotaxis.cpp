//Random path recording a random trajectory of actor 
//plus the correct vectors in each position
#include "Chemotaxis.h"

void Chemotaxis::SetNNInput() {
	//I have to probably iterate through the receptors' positions and calculating gradients 
	//The result must be modified Poisson input/output, so here we set up where the neurons will come looking for it
}